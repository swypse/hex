/** Shared vertical rhythm for form screens (game setup and lobby host):
 *  distance from a heading's centre down to its block content, and the gap
 *  between the bottom of one block and the centre of the next heading. */
export const TITLE_TO_CONTENT = 22;
export const BLOCK_GAP = 20;